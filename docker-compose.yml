services:
  api:
    build: ./api
    container_name: api
    expose:
      - "8080"
    environment:
      - API_PROPERTIES_APPLICATION_PRIVATE_KEY_PATH=/keys/jwt-private.pem
      - API_PROPERTIES_APPLICATION_PUBLIC_KEY_PATH=/keys/jwt-public.pem
      - RSA_PRIVATE_CONTENT=${JWT_PRIVATE_KEY_CONTENT}
      - RSA_PUBLIC_CONTENT=${JWT_PUBLIC_KEY_CONTENT}
      - PROFILE_ACTIVE=${PROFILE_ACTIVE}
      - WEB_URL=${WEB_URL}
      - API_URL=${API_URL}
      - API_URL_SERVER=${API_URL_SERVER}
      - POSTGRES_HOST=database
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - MAILER_HOST=${MAILER_HOST}
      - MAILER_PORT=${MAILER_PORT}
      - MAILER_USERNAME=${MAILER_USERNAME}
      - MAILER_PASSWORD=${MAILER_PASSWORD}
      - MAILER_FROM_SMTP=${MAILER_FROM_SMTP}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET}
    entrypoint: >
      sh -c "mkdir -p /keys && 
             echo \"$$RSA_PRIVATE_CONTENT\" > /keys/jwt-private.pem && 
             echo \"$$RSA_PUBLIC_CONTENT\" > /keys/jwt-public.pem && 
             exec java -jar app.jar"
    depends_on:
      - database
    networks:
      - local-network

  web:
    build:
      context: ./web
      args:
        - WEB_URL=${WEB_URL}
        - API_URL=${API_URL}
        - API_URL_SERVER=${API_URL_SERVER}
    container_name: web
    expose:
      - "3000"
    networks:
      - local-network

  database:
    image: postgres:17-alpine
    container_name: database
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    expose:
      - "5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - local-network

  admin:
    image: dpage/pgadmin4:latest
    container_name: admin
    environment:
      - PGADMIN_DEFAULT_EMAIL=${PGADMIN_DEFAULT_EMAIL}
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_DEFAULT_PASSWORD}
      - PGADMIN_CONFIG_ENHANCED_COOKIE_PROTECTION=False
      - PGADMIN_LISTEN_PORT=80
    ports:
      - "80"
    depends_on:
      - database
    networks:
      - local-network

networks:
  local-network:
    driver: bridge

volumes:
  pgdata:
